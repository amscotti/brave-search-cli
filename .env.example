# Copy this file to .env and fill in your real key.
# The real .env is untracked and must never be committed.
# The CLI reads BRAVE_API_KEY from the environment (or the secure config file);
# it never reads .env files itself.
BRAVE_API_KEY=
