{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://brave-search-cli.invalid/schemas/v1/web-result.schema.json",
  "title": "web result record (v1)",
  "description": "One logical web search result as one JSONL result record: the original zero-based position in the upstream web.results array, the constant web bucket, and the usable textual members the upstream result carried. Absent members are omitted, never emitted as null.",
  "type": "object",
  "properties": {
    "schema_version": {
      "const": "1"
    },
    "type": {
      "const": "result"
    },
    "command": {
      "const": "web"
    },
    "bucket": {
      "const": "web"
    },
    "position": {
      "type": "integer",
      "minimum": 0
    },
    "title": {
      "type": "string"
    },
    "url": {
      "type": "string"
    },
    "description": {
      "type": "string"
    }
  },
  "required": ["schema_version", "type", "command", "bucket", "position"],
  "additionalProperties": false
}
