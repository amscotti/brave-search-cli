/** Human-only config commands consuming only configuration ports; input adapters stay behind
 * injectable functional seams so no command references a concrete config adapter. */
package io.amscotti.bravesearch.adapter.cli.command.config;
