/**
 * The web search command of the CLI adapter: one command class owning the query positional
 * and the endpoint's option grammar, driving injected ports for credentials, exchange, and
 * presentation. The package holds no transport or presentation types of its own.
 */
package io.amscotti.bravesearch.adapter.cli.command.web;
