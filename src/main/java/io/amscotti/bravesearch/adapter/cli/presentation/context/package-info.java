/**
 * The LLM context renderer specialization of the shared presenter skeleton: the context
 * projection extractor and the presenter whose seams carry the context counts and the
 * single-record JSONL shape.
 */
package io.amscotti.bravesearch.adapter.cli.presentation.context;
